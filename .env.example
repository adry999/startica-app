# Supabase (local: from `supabase start` output; prod: from dashboard)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Brevo (email) — get from Brevo dashboard when you reach invitations
BREVO_API_KEY=
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=
BREVO_SMTP_PASS=
EMAIL_FROM="Startica <no-reply@yourdomain.tld>"

# App
DEFAULT_LOCALE=ro
